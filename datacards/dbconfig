# Default configuration file for DbImage's
# Copy this file and set it to the DBCONFIG environment variable

# mind the space around the :
ImagePath
{
here : .
#cfht09 : /data/cfht/megacam09*
}

# where to find astrometric catalogs (non-USNO catalogs usually)
CatalogPath
{
  .
#  /data/my_catalogs
#  /data/my_od/catalogs
#  /data/catalogs/D*
}

# what are the image names for the various DbImage derived classes
# .fits : regular fits image
# .fz : rice compressed fits image
# .fits.gz gzip compression
# mind the spaces around { and }

ImageNames
{
#default if not overwritten:
  { calibrated.fz satur.fits.gz weight.fits }
#  ImageSum { calibrated.fz satur.fits.gz  weight.fits }
#  TransformedImage { calibrated.fz satur.fits.gz weight.fits }
#  SwarpStack { calibrated.fz satur.fits.gz weight.fits }
#  SubImage { calibrated.fz satur.fits.gz weight.fits }
#  ImageSubtraction { calibrated.fz satur.fits.gz weight.fits }
}
