# Default configuration file for DbImage's
# Copy this file and set it to the POLOKA_DB_CONFIG environment variable

# mind the space around the :
ImagePath
{
here : .
#cfht09 : /data/cfht/megacam09*
}

# where to find astrometric catalogs (non-USNO catalogs usually)
CatalogPath
{
  .
#  /data/my_catalogs
#  /data/my_od/catalogs
#  /data/catalogs/D*
}

# what are the image names for the various DbImage derived classes
# .fits : regular fits image
# .fz : rice compressed fits image
# .fits.gz gzip compression
# mind the spaces around { and }

ImageNames
{
# default if not overwritten:
  { calibrated.fits.fz satur.fits.gz weight.fits.fz }
#  ImageSum { calibrated.fits.fz satur.fits.gz  weight.fits }
#  TransformedImage { calibrated.fits.fz satur.fits.gz weight.fits }
#  SwarpStack { calibrated.fits.fz satur.fits.gz weight.fits }
#  SubImage { calibrated.fits.fz satur.fits.gz weight.fits }
#  ImageSubtraction { calibrated.fits.fz satur.fits.gz weight.fits }
}
