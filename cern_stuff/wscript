# -*- mode: python -*-
# 



def build( bld ):
    
    sources = bld.path.ant_glob('*.F')
        
    lib = bld(features = 'fc fcshlib', 
              source = sources,
              target = 'cern_stuff', 
              vnum     = '0.1.0')

    
